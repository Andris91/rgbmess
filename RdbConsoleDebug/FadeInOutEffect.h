#pragma once
#include "stdafx.h"
/*
class FadeInOutEffect {
public:

	FadeInOutEffect(hsv color, double step, DWORD delay);

	void start();
	void start(KeyRegion keyRegion);
	void stop();

private:
	hsv color;
	double step;
	DWORD delay;

	boost::thread effectThread;
	boolean shouldThreadStop = false;

	void loop(KeyRegion* keyRegion);
};
*/